namespace Alkiman.Domain.Enums;

/// <summary>Tipo de pedido que el cliente hace desde su link público de renta (columna TRX_RentalRequests.Type).</summary>
public enum RentalRequestType
{
    Extension,
    Cancellation
}
