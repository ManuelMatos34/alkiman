# Copiar a .env.development (dev) o .env.production (prod) y ajustar los valores.
# .env.production NO debe commitearse — agrégalo a .gitignore.
VITE_API_BASE_URL=https://localhost:7083
VITE_APP_ENV=development
